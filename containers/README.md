# Souporcell Container

The default config expects this image path:

```bash
containers/souporcell.sif
```

Build it with:

```bash
containers/build_singularity_image.sh
```

Use `remote` if you need a remote builder:

```bash
containers/build_singularity_image.sh remote
```
